#include <stdio.h>
int main(){
    int n,i,j,arr[100],sum;
    scanf("%d",&n);
    for(i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    for(i=0;i<n-1;i++){
        for(j=1;j<n;j++){
            sum=arr[j]+arr[j+1];
            if(sum>)
        }
    }
}